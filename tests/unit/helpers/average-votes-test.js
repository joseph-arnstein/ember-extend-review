import { averageVotes } from 'shopping-cart/helpers/average-votes';
import { module, test } from 'qunit';

module('Unit | Helper | average votes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = averageVotes([42]);
  assert.ok(result);
});

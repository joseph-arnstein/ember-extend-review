import { addCost } from 'shopping-cart/helpers/add-cost';
import { module, test } from 'qunit';

module('Unit | Helper | add cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addCost([42]);
  assert.ok(result);
});

import { showAverage } from 'shopping-cart/helpers/show-average';
import { module, test } from 'qunit';

module('Unit | Helper | show average');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showAverage([42]);
  assert.ok(result);
});

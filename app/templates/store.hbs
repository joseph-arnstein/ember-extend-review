<div class="jumbotron">
<h1>HI this is in store hbs</h1>
</div>

<div class="container">
  <div class="row">
    {{#each model.products as |product|}}
      {{product-tile product=product saveCommentD='saveCommentE'}}
    {{/each}}
  </div>
</div>

{{new-product  saveB='saveC'}}

{{display-cart}}

{{total-cost}}
